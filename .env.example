# =============================================================================
# BBoS (Balochistan Statistics Navigator) - Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your actual values
# NEVER commit .env to version control!

# =============================================================================
# APPLICATION SETTINGS
# =============================================================================
NODE_ENV=production
# Options: development, production

PORT=5001
# Port for the application
# Development (Replit): 5000
# Production (multi-app server): 5001, 5002, etc. (choose an available port)

HOST=127.0.0.1
# Development: 0.0.0.0 (all interfaces)
# Production: 127.0.0.1 (only accessible via Apache/Nginx proxy)

# =============================================================================
# DATABASE CONFIGURATION (Aiven PostgreSQL)
# =============================================================================
# Your Aiven PostgreSQL connection string
DATABASE_URL=postgresql://username:password@host:port/database?sslmode=require
REMOTE_DATABASE_URL=postgresql://username:password@host:port/database?sslmode=require

# Example for Aiven:
# DATABASE_URL=postgresql://avnadmin:xxxxx@pg-xxx-xxx.aivencloud.com:14517/bbos?sslmode=require
# REMOTE_DATABASE_URL=postgresql://avnadmin:xxxxx@pg-xxx-xxx.aivencloud.com:14517/bbos?sslmode=require

# Individual connection details (for reference)
PGHOST=your-aiven-host.aivencloud.com
PGPORT=14517
PGUSER=avnadmin
PGPASSWORD=your-secure-password
PGDATABASE=bbos

# =============================================================================
# SESSION SECURITY
# =============================================================================
SESSION_SECRET=generate-a-strong-random-secret-key-here
# IMPORTANT: Generate a secure random string for production!
# Generate using: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# Example output: a1b2c3d4e5f6... (64 characters)

# =============================================================================
# OPTIONAL OVERRIDES (usually not needed)
# =============================================================================
# Only uncomment and modify if you need to change defaults

# SERVER_TIMEOUT=60000           # Server timeout in milliseconds (default: 60000)
# SESSION_TTL=86400              # Session duration in seconds (default: 86400 = 24 hours)
